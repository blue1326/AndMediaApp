using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Button_ActiveDeactive : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField]
    GameObject[] Objects;


    private void Awake()
    {
        
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
